# WORKFLOW_MIGRATION.md - Migrate the existing repository authority

## 0. Purpose

This pack is a governance and execution-workflow migration for the existing `lucakollmer/opening-trainer` repository. It does not replace the PHASE-0 application candidate.

The migration corrects a known authority conflict: the original repository pack uses the superseded local-executor evidence policy, while the accepted project workflow requires ChatGPT-authored GitHub changes and Actions-based technical evidence.

## 1. Required starting state

Reverify before writing:

```yaml
repository: lucakollmer/opening-trainer
visibility_observed: public
base_branch: main
base_sha: 87ccbce18384892601a6630494910e1ca0375f13
working_branch: phase-0-foundation
expected_pre_migration_head: 5477419fce1f13f4265ab82d6ee3058d851b5019
pull_request: 2
pull_request_state: open
pull_request_draft: true
pull_request_merged: false
permanent_validation_run: 30906801969
permanent_validation_conclusion: success
manual_phase_acceptance: pending
```

Stop before mutation if any mutable fact differs. Report the observed state and ask Luca whether to reconcile or regenerate this migration against the new head.

## 2. Branch and PR rule

Apply this migration on the existing `phase-0-foundation` branch and draft PR #2.

Do not:

- create a new migration branch or PR;
- write to `main`;
- merge PR #2;
- begin PHASE-1;
- rerun the original PHASE-0 scaffold;
- modify repository visibility, branch protection, secrets, or permissions.

## 3. Preserve exactly

Preserve all current PHASE-0 application implementation unless a concrete migration defect requires a separately reported correction. In particular preserve:

```text
.github/workflows/ci.yml
package.json
pnpm-lock.yaml
pnpm-workspace.yaml
vite.config.ts
eslint.config.js
prettier.config.mjs
index.html
src/**
tsconfig.app.json
tsconfig.json
tsconfig.node.json
docs/architecture/PHASE-0-BOUNDARIES.md
docs/dependencies/INSTALLED_DEPENDENCIES.md
docs/ui/PHASE-0-REUSE-INVENTORY.md
scripts/check-pwa.mjs
scripts/update-integrity-manifest.mjs
scripts/write-dependency-record.mjs
```

Do not replace the current repository `SHA256SUMS.txt` with this pack's archive checksum file. The repository's integrity manifest covers the full candidate tree and must be regenerated by its existing script after migration.

## 4. Replace from this pack

Replace these repository paths with the pack versions:

```text
AGENTS.md
context.md
plans.md
README.md
START_DEVELOPMENT.md
.github/pull_request_template.md
docs/product/PRODUCT_CONTRACT.md
docs/testing/TEST_AND_ACCEPTANCE_STRATEGY.md
docs/ui/UI_AND_INTERACTION_CONTRACT.md
docs/decisions/DECISIONS.md
docs/dependencies/VERIFIED_CANDIDATES_2026-08-03.md
prompts/README.md
scripts/verify-pack.mjs
PACK_MANIFEST.md
```

When replacing `scripts/verify-pack.mjs`, preserve the repository-integrity role added during PHASE-0. Use the migration pack implementation only if it verifies both the required governance files and the existing full-tree integrity manifest. Do not weaken current integrity checks.

## 5. Add from this pack

Add:

```text
CHATGPT_START_HERE.md
CHATGPT_WORKFLOW_PROFILE.md
WORKFLOW_MIGRATION.md
docs/workflow/CHATGPT_GITHUB_ACTIONS_CODING.md
docs/workflow/PHASE_COMMANDS.md
docs/workflow/PROMPT_WRITING_GUIDELINES.md
docs/workflow/STRUCTURED_REPORT_TEMPLATE.md
docs/workflow/ACTIONS_SECURITY_AND_EVIDENCE.md
prompts/WRK-OPENING-TRAINER-20260804-001__MVP__GOVERNANCE__migration__v1.md
```

## 6. Delete deprecated paths

Delete only these obsolete execution-control paths:

```text
CODEX_START_HERE.md
CODEX_PROMPT_PROFILE.md
docs/codex/COMPLETION_REPORT_TEMPLATE.md
docs/codex/PHASE_COMMANDS.md
docs/codex/PROMPT_WRITING_GUIDELINES.md
prompts/PRM-OPENING-TRAINER-20260803-001__MVP__PHASE-0__implementation__v1.md
```

Do not delete `CODEX_HANDOFF.md` from Google Drive. That filename is historical and its latest section correctly records the superseding workflow.

## 7. Repository integrity update

After file changes:

1. use the existing `scripts/update-integrity-manifest.mjs` to regenerate the repository-wide `SHA256SUMS.txt`;
2. run the repository's existing integrity verifier;
3. ensure no stale `docs/codex` or root `CODEX_*` authority path remains;
4. ensure no text still says GitHub Actions is incidental or that local validation is the technical authority;
5. ensure no product/application file changed unexpectedly.

Commit the new `PACK_MANIFEST.md` as the governance-overlay provenance record. The transfer archive `SHA256SUMS.txt` is not copied over the repository version. Regenerate the repository-wide `SHA256SUMS.txt` after all changes.

## 8. Required Actions validation

Push the bounded migration commit to `phase-0-foundation`. The permanent read-only Actions workflow must validate the exact new head.

Required evidence:

- repository integrity pass;
- lint pass;
- strict typecheck pass;
- deterministic tests pass;
- production build pass;
- PWA check pass;
- formatting and whitespace pass;
- exact changed-file list;
- statement that application code and dependencies were preserved;
- updated candidate head SHA.

A screenshot rerun is optional because this migration should not change visible code. If any visible file changes, screenshot evidence becomes required.

## 9. PR update

Update draft PR #2 to state:

- PHASE-0 implementation remains unchanged;
- repository governance now follows ChatGPT + GitHub Actions;
- old Codex/local validation authority was removed;
- permanent Actions validation passed at the new head;
- Luca's four-item manual PHASE-0 checklist remains pending;
- PR stays draft, unmerged, and PHASE-1 remains blocked.

## 10. Completion status

A successful migration ends with:

```text
COMPLETE_FOR_MANUAL_REVIEW
```

The exact next action is Luca's PHASE-0 manual checklist and explicit acceptance or rejection. Do not merge or continue automatically.
